import { PowerFormatPipe } from './power-format.pipe';

describe('PowerFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
